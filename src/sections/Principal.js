const Principal = () => {
  return (
    <div className="principal" style={{textTrasform: "lowercase!import"}}>
      <h2>sobre el prototipo</h2>
      <p>
        Por ahora solo se ha desarrollado un pequeño ejemplo de listado en la
        sección <b>listado de pacientes</b> además que se puede accedar a un
        apenas maquetado de la <b>Ficha de paciente</b>{" "}
      </p>
    </div>
  );
};

export default Principal;
